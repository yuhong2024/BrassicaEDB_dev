<template>
  <div class="app">
    <Header />
    <main class="main-content">
      <RouterView />
    </main>
    <footer>
      <FooterLinks />
         <Footer />
    </footer>
  </div>
</template>

<script setup>
import Header from './components/Base/Header.vue';
import Footer from './components/Base/Footer.vue';
import FooterLinks from './components/Base/FooterLinks.vue';
import { RouterView } from 'vue-router';
</script>

<style scoped>
.app {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.main-content {
  flex: 1;
  width: 90vw; /* 调整整体宽度 */
  max-width: 1200px; /* 控制最大宽度 */
  margin: 0 auto; /* 居中显示 */
  padding-top: 70px; /* 根据header高度调整 */
  padding-bottom: 60px; /* 根据footer高度调整 */
  box-sizing: border-box;
}

/* 确保footer部分与其他部分一致 */
footer {
  width: 90vw; /* 与 main-content 一致 */
  max-width: 1200px; /* 与 main-content 一致 */
  margin: 0 auto; /* 居中显示 */
  padding-bottom: 40px; /* 增加padding与页面整体风格统一 */
  box-sizing: border-box;
}
</style>
